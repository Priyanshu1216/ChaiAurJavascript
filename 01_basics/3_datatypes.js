"use strict"; //it means all JS code will be used in strict mode(newer version)


// alert("hell") we're using NodeJs and not browser so it won't run

// Datatypes

//number => 2 to the power 53
//string => ""
//boolean => true/false
//null => standalone value (representational of empty value)
//undefined => which is not been defined yet
//symbol => used for uniqueness



// Object => {key: value}


console.log(typeof("rashu")); //typeof is used to find the type of variable

console.log(typeof(null)); //output = object
console.log(typeof(undefined)); //output = undefined because undefined is a type


